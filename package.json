{
  "name": "cafe-vesuvius",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd backend && npm install",
    "dev": "docker compose -f ./docker/docker-compose.yml up --watch",
    "build:dev": "docker compose -f ./docker/docker-compose.yml build"
  },
  "author": "Vesuvius",
  "description": "World best Cafe management system",
  "dependencies": {
    "ts-node": "^10.9.2"
  }
}